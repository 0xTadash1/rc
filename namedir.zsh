hash -d xconf="$XDG_CONFIG_HOME"
hash -d xcache="$XDG_CACHE_HOME"
hash -d xdata="$XDG_DATA_HOME"
hash -d xstate="$XDG_STATE_HOME"

hash -d zsh="$ZDOTDIR"

hash -d pj="$HOME/Projects"

hash -d nvimswap="$XDG_STATE_HOME/nvim/swap"
